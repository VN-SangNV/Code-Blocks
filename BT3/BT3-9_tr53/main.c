/*
Trong mặt phẳng tọa độ OXY cho hai hình chữ nhật có cạnh song song với các trục tọa độ,
hình chữ nhật THỨ NHẤT có tọa độ gốc dưới trái là A(X1, Y1) và tọa độ góc trên phải là B(X2, Y2).
Hình chữ nhật THỨ HAI có tọa độ góc dưới trái là C(X3, Y3) và tọa độ gốc trên phải D(X4, Y4).
Các tọa độ là số nguyên. Hãy tìm phần diện tích giao nhau của 2 hình chữ nhật.
*/
#include <stdio.h>
#include <stdlib.h>
void answer(){
    int x1, x2, x3, x4, y1, y2, y3, y4;

    printf("Nhap diem A(x, y): ");
    scanf("%d%d", &x1, &y1);
    printf("Nhap diem B(x, y): ");
    scanf("%d%d", &x2, &y2);
    printf("Nhap diem C(x, y): ");
    scanf("%d%d", &x3, &y3);
    printf("Nhap diem D(x, y): ");
    scanf("%d%d", &x4, &y4);


}

int main()
{
    printf("Hello world!\n");
    return 0;
}
